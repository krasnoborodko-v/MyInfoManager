# Основные зависимости
# sqlite3 - встроено в Python, установка не требуется

# Для тестирования
pytest>=7.0.0

# Для веб-сервера
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6  # Для загрузки файлов

# Для синхронизации с веб-сервером
requests>=2.31.0
httpx>=0.24.0

# Для упаковки в EXE
pyinstaller>=6.0.0

# PostgreSQL (для сервера)
psycopg2-binary>=2.9.0

# Авторизация
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
